# Neon Postgres
DATABASE_URL="postgresql://user:password@ep-example.neon.tech/vst_calibration?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-example.neon.tech/vst_calibration?sslmode=require"

# NextAuth
AUTH_SECRET="run: openssl rand -base64 32"
AUTH_URL="http://localhost:3000"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Vercel Blob (para firmas y PDFs - se agrega en Sprint 4-5)
# BLOB_READ_WRITE_TOKEN=""

# Solo para ejecutar `npm run db:seed` en una base nueva.
# Usa contraseñas distintas, fuertes y de al menos 12 caracteres.
SEED_ENGINEER_PASSWORD=""
SEED_ADMIN_PASSWORD=""
